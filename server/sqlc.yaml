version: "2"
sql:
  - engine: "sqlite"
    queries: "pkg/db/queries/"
    schema: "migrations-sqlite/"
    gen:
      go:
        package: "db"
        out: "pkg/db/generated"
        emit_json_tags: true
        emit_empty_slices: true
